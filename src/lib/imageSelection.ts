import { imageFiles } from "@/lib/images";

export const heroImages = imageFiles.slice(0, 4);
export const homeGalleryImages = imageFiles.slice(4, 10);
export const houseTopImages = imageFiles.slice(10, 14);
export const houseGalleryImages = imageFiles.slice(14, 22);
export const studioGalleryImages = imageFiles.slice(22, 28);
